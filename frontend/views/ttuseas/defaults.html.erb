<%= setup_context(:object => @ttusea, :title => I18n.t("actions.defaults")) %>

<%= form_for @ttusea, :as => "ttusea", :url => {:action => :update_defaults}, :html => {:class => 'form-horizontal aspace-record-form', :id => "ttusea_form"} do |f| %>

  <%= form_context :ttusea, @ttusea do |form| %>
    <div class="row">
      <div class="col-md-3">
        <%= render_aspace_partial :partial => "sidebar" %>
      </div>
      <div class="col-md-9">
        <%= render_aspace_partial :partial => "shared/toolbar_new_records" %>
        <div class="record-pane">
          <%= link_to_help :topic => "ttusea" %>
          <h2><%= I18n.t("ttusea._singular") %> <%= I18n.t("actions.defaults") %> <span class="label label-info"><%= I18n.t("ttusea._singular") %></span></h2>
          <%= render_aspace_partial :partial => "ttuseas/form", :locals => {:form => form} %>
        </div>
      </div>
    </div>
  <% end %>
<% end %>
