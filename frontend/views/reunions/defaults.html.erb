<%= setup_context(:object => @reunion, :title => I18n.t("actions.defaults")) %>

<%= form_for @reunion, :as => "reunion", :url => {:action => :update_defaults}, :html => {:class => 'form-horizontal aspace-record-form', :id => "reunion_form"} do |f| %>

  <%= form_context :reunion, @reunion do |form| %>
    <div class="row">
      <div class="col-md-3">
        <%= render_aspace_partial :partial => "sidebar" %>
      </div>
      <div class="col-md-9">
        <%= render_aspace_partial :partial => "shared/toolbar_new_records" %>
        <div class="record-pane">
          <%= link_to_help :topic => "reunion" %>
          <h2><%= I18n.t("reunion._singular") %> <%= I18n.t("actions.defaults") %> <span class="label label-info"><%= I18n.t("reunion._singular") %></span></h2>
          <%= render_aspace_partial :partial => "reunions/form", :locals => {:form => form} %>
        </div>
      </div>
    </div>
  <% end %>
<% end %>
