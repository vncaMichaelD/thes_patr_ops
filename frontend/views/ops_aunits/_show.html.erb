<%
   section_id = "ops_aunits" if section_id.blank?
%>

<section id="<%= section_id %>">
  <h3><%= I18n.t("ops_aunit._plural") %></h3>
  <div class="accordion details" id="<%= section_id %>_accordion">
    <% ops_aunits.each_with_index do | ops_aunit, index | %>
      <div class="panel panel-default">
        <div class="panel-heading">
          <div class="row accordion-toggle collapsed" data-toggle="collapse" data-parent="#<%= section_id %>_accordion" href="#<%= section_id %>_ops_aunit_<%= index %>">
            <div class="col-md-7">
              <%= ops_aunit['unit'] %>
            </div>
          </div>
        </div>
        <div id="<%= section_id %>_ops_aunit_<%= index %>" class="accordion-body collapse">
          <%= read_only_view(ops_aunit) %>
        </div>
      </div>
    <% end %>
  </div>
</section>
